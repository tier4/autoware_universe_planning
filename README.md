## TIER IV universe / split mirror (planning)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe:/planning/` into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See the beta branches for source codes.
